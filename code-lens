#!/usr/bin/env bash

set -u

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd -P)
cd "$SCRIPT_DIR"

command -v uv >/dev/null 2>&1 || { printf 'Error: uv is required: https://docs.astral.sh/uv/\n' >&2; exit 1; }

exec uv run --project backend codelens-review "$@"
