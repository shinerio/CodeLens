上一轮最终输出因不符合结构化格式而被拒绝。只能将其中有受控历史证据支持的审查意见转换为以下 schema；证据标识必须已在提供的历史中出现，禁止补造缺失证据。若没有任何意见能被忠实表示，返回空的 findings 数组。

只能输出一个符合以下 schema 的 JSON 对象。不得输出说明文字、Markdown、代码围栏、解释、文件列表或任何其他文本。

{{finding_batch_schema}}
