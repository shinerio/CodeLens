The preceding final output was rejected because it was not valid structured output. Convert only its supported review findings into the schema below, using evidence identifiers already present in the supplied history. Do not invent missing evidence. If no finding can be represented faithfully, return an empty findings array.

Output exactly one JSON object matching this schema. Do not include prose, Markdown, code fences, explanations, file listings, or any other text.

{{finding_batch_schema}}
