Your review finding does not fall on an actual changed diff line. Focus on this Review's code changes and submit the finding on consecutive removed lines on the old side or added lines on the new side. Set `existing_code` to only those changed lines, without unchanged context. If the issue is indirectly caused by this change (e.g., a comment became stale due to new code), re-anchor `existing_code` to the nearest changed lines (new-side additions or old-side removals) and explain the relationship in `content`.
