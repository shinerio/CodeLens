# CodeLens 产品白皮书

## 摘要

CodeLens 是一个面向本地代码仓库的开源智能代码审查工具。它把依赖个人经验、容易遗漏且耗时较长的代码审查，转变为由多个专业 Reviewer 协同完成、结论有依据、结果可追溯、过程受用户控制的日常工作方式。

CodeLens 的职责是扩大审查覆盖面，尽早发现风险，解释问题影响并提供可执行的改进建议。项目对仓库内容保持只读，用户根据 Findings 自行决定后续行动。

首个可用版本以单个用户在自己的设备上使用为核心场景，同时支持多个审查任务并行进行。任务可以来自不同仓库，也可以针对同一仓库中的不同功能变更。每个任务都拥有独立且固定的审查输入，同一任务中的多个 Reviewer 可以同时工作。

## 1. 项目缘起

### 1.1 审查视角容易受限

一次变更可能同时涉及功能正确性、安全性、性能、可维护性、测试质量、文档规范和上下游影响。审查者很难在有限时间内持续覆盖所有方向。

### 1.2 审查标准分散

项目约定可能存在于说明文档、目录规则、文件规则和维护者经验中。规则一旦分散，审查结果就容易因人、因时间和上下文而变化。

### 1.3 审查意见缺乏可验证依据

高质量审查意见需要指出准确位置、问题成因、影响范围、可信依据和改进方向。缺少这些信息的结论会增加沟通成本并造成误报疲劳。

### 1.4 大型与并行场景放大等待成本

当多个功能同时开发或一次变更涉及较多文件时，串行审查会延长反馈周期。审查输入若受到其他活动任务影响，结论的可信度也会下降。

CodeLens 由此出发：让智能审查成为专业分工明确、证据充分、边界清晰并由人掌控的只读辅助能力。

## 2. 项目使命与定位

CodeLens 的使命是帮助开发者和开源项目维护者更早、更全面、更有把握地理解代码变更风险。

项目遵循以下目标：

- 让不同规模的项目获得多角度、可重复的代码审查。
- 让每条重要结论都能追溯到代码位置、项目规则和审查依据。
- 明确展示已检查内容、覆盖缺口和 Reviewer 失败状态。
- 保持仓库、审查工具与模型运行时的只读边界。
- 让审查经验逐步沉淀，同时防止单次反馈直接改变长期规则。

CodeLens 是开发者身边的多 Reviewer 审查工作台，不是替代人工判断的自动裁决者。完整流程是：

> 选择审查内容 → 选择 Reviewer → 获得有依据的 Findings → 理解整体风险 → 反馈并沉淀规则

## 3. 目标用户

### 3.1 独立开发者

在提交或发布前获得多个专业视角的第二意见，发现因熟悉代码而容易忽略的问题。

### 3.2 开源项目维护者

统一审查标准、整理风险重点并减少重复检查，把注意力放在项目方向和关键判断上。

### 3.3 开源项目贡献者

在发起合入请求前检查变更是否符合项目规则，提高变更质量并减少往返沟通。

### 3.4 审查陌生变更的开发者

梳理跨文件变更的问题位置、上下游影响和风险来源，降低理解成本。

## 4. 典型使用场景

### 4.1 合入前检查

选择功能相关变化，启动全部或部分 Reviewer，在提交给维护者前识别高风险问题。

### 4.2 未提交内容检查

检查仍在编辑中的 staged、unstaged 和未忽略的 untracked 内容，在正式提交前获得反馈。

### 4.3 历史基准比较

指定历史基准与目标版本，检查两者之间的变化，适用于版本比较、回归检查或长周期开发。

### 4.4 完整仓库检查

按区域检查完整仓库。若受范围或资源限制无法覆盖全部内容，报告必须明确列出未覆盖部分。

### 4.5 多任务并行审查

同时审查多个仓库，或同一仓库中的不同功能变更。每项任务面对各自冻结的输入，彼此不会混入变化。

## 5. 完整产品流程

### 5.1 选择仓库与范围

用户从以下范围中选择一种：

- 两个开发基准之间的变化。
- 从指定历史基准到目标版本的变化。
- 尚未提交的本地变化。
- 完整代码仓库。

启动前展示包含与排除内容，避免在范围不明确时开始审查。

### 5.2 选择 Reviewer

系统默认选择全部已启用的 Reviewer，用户也可以只选择本次需要的专业视角。每个 Reviewer 保持独立职责。

### 5.3 读取项目规则

CodeLens 把项目级、目录级和文件级审查要求应用到相应内容。平台的安全、只读、证据和输出规范不能被仓库内容取消。

### 5.4 并行执行

同一任务中的 Reviewer 面对相同且固定的审查输入。单个 Reviewer 失败不会抹去其他 Reviewer 已完成的结果；存在有效结果时，系统生成部分完成报告并说明缺失视角。

### 5.5 验证与汇总 Findings

CodeLens 检查问题位置和依据是否有效，对重要或置信度不足的问题进一步核验，并合并重复结论。汇总阶段只能整理已经验证的 Findings。

### 5.6 阅读报告与反馈

用户可以按严重程度、类别、置信度、来源、Reviewer、文件和处理状态筛选结果。每条 Finding 说明：

- 问题位置。
- 风险原因与可能影响。
- 证据和项目规则来源。
- 问题与目标变化的关系。
- 建议采取的行动。

用户可以接受、忽略、标记误报或提出规则建议。反馈先形成候选建议，经过确认后才能成为长期规则。

## 6. 功能体系

### 6.1 多范围代码审查

CodeLens 支持从小范围变化到完整仓库的检查方式。所有输入在任务开始时冻结，Reviewer 不读取可变的原始工作区。

### 6.2 专业 Reviewer

规划提供以下默认 Reviewer：

1. **正确性**：逻辑错误、边界条件、异常处理、状态变化和并发问题。
2. **安全性**：权限、输入风险、敏感信息、数据暴露和依赖来源。
3. **性能**：重复工作、响应退化、资源浪费和规模化风险。
4. **可维护性**：职责、耦合、重复、契约和后续演进成本。
5. **测试**：回归风险、边界用例、失败路径和覆盖有效性。
6. **文档与规范**：公开说明、命名、文档完整性和项目约定。
7. **跨文件影响**：接口变化、调用关系和上下游连锁影响。

### 6.3 分层项目规则

审查要求可以作用于整个项目、目录、具体文件或特定 Reviewer。报告展示命中的规则和来源。

### 6.4 有证据的 Findings

Finding 包含类别、严重程度、置信度、准确位置、影响、解释、证据、建议和处理状态。系统拒绝位置无效或依据无法对应的输出。

### 6.5 综合报告

综合报告展示审查范围、Reviewer 状态、风险排序、覆盖情况、证据、规则、耗时、资源使用和失败信息。报告只做排序、归类、去重和解释。

### 6.6 运行记录与过程可见性

用户可以查看活动与已完成任务，了解 Reviewer 状态、耗时、覆盖和失败原因。任务取消后不再启动新的检查。

### 6.7 专业能力扩展

用户可以为特定领域补充审查知识、项目说明、检查工具和外部只读信息来源。每个 Reviewer 只能使用明确分配的能力，扩展不可用时必须说明覆盖缺口。

### 6.8 质量评测

可重复评测关注有效问题发现能力、误报与重复比例、高风险问题可信度、覆盖范围、耗时和资源使用。明显改变审查行为的调整应先与既有基准比较。

## 7. 核心产品特性

### 7.1 多视角

不同 Reviewer 独立检查同一变化，用户既能看到综合判断，也能追溯每个 Reviewer 的原始结论。

### 7.2 证据优先

重要结论必须关联位置、依据、影响和规则来源；无法证明的结论会被拒绝或降低可信度。

### 7.3 范围透明

系统明确说明检查内容、跳过内容、失败 Reviewer 和覆盖缺口，不把部分成功包装成完整成功。

### 7.4 并行且独立

多 Reviewer 和多任务可以并行工作，每个任务都基于自己的固定输入。

### 7.5 全程只读

源仓库、Git 状态和任务 worktree 均不向 Reviewer 提供写权限。模型工具只读取冻结 Snapshot，Findings 只表达审查意见与建议。

### 7.6 失败可见

单个 Reviewer 失败不自动取消其他 Reviewer。系统保留有效结果，并清楚说明失败节点和报告完整程度。

### 7.7 隐私和权限边界明确

代码内容、项目规则和扩展信息均被视为敏感且不可信的数据。仓库内容不能借审查过程扩大权限。

## 8. 可信产品原则

1. **事实可追溯**：重要结论可以追溯到位置、依据和规则来源。
2. **范围要诚实**：明确披露未覆盖、失败和不确定性。
3. **输入要稳定**：任务开始后审查对象保持固定。
4. **仓库只读**：审查过程不改变仓库内容或 Git 状态。
5. **权限不可扩大**：代码、规则和扩展内容不能增加自身能力。
6. **失败不掩盖**：不因汇总方便隐藏 Reviewer 失败。
7. **反馈不直接改变规则**：反馈先形成建议，再由用户确认。
8. **质量变化先验证**：审查行为变化必须经过对比评测。
9. **人承担最终判断**：项目不替代人工完成业务判断、设计取舍或合入决策。

## 9. 能力边界

- CodeLens 是代码审查辅助工具，不是绝对无缺陷的证明工具。
- 它可以指出潜在业务逻辑风险，但不能代替领域专家确认真实业务意图。
- 它可以提供设计和维护意见，但不替项目维护者决定长期方向。
- 完整仓库检查可能受到范围和资源限制，此时必须报告覆盖缺口。
- 首个可用版本面向单用户本地使用。
- 用户的一次反馈不会直接改变长期审查规则。
- 审查能力不会扩展到与所选仓库无关的资源。

## 10. 产品发展方向

### 第一阶段：明确审查对象

准确选择仓库和审查范围，预览包含与排除内容，确保并行任务互不影响。

### 第二阶段：形成基础审查闭环

完成创建任务、执行 Reviewer、生成有位置和证据的 Findings，以及查看运行状态和结果。

### 第三阶段：实现多 Reviewer 协同

支持并行执行、证据核验、问题去重、部分成功和综合报告。

### 第四阶段：增强个性化能力

允许用户扩展审查知识、项目规则、Reviewer 和外部只读信息来源，并让权限和覆盖缺口保持可见。

### 第五阶段：提高安全性与易用性

完善本地体验、数据保护、故障恢复、信息清理和不同设备环境下的一致性。

### 第六阶段：建立持续质量基准

通过公开、可重复的评测持续观察有效发现、误报、覆盖、速度和资源使用，防止产品更新导致审查质量退化。

## 11. 开源项目方向

CodeLens 希望把代码审查方法沉淀为可公开讨论、验证和改进的共同成果。项目重点维护清晰的能力边界、可讨论的 Reviewer 与项目规则、可重复的质量评测，以及对误报、遗漏、权限风险和失败行为的公开反馈机制。

社区贡献可以是功能开发、审查规则、问题样例、质量评测、文档改进、专业知识和真实使用反馈。

## 结语

CodeLens 希望建立一种更可信的代码审查方式：多个专业视角可以同时参与，每个重要结论尽量有依据，审查范围和失败状态对用户透明，多项开发工作并行推进且互不干扰，项目经验能够持续沉淀。

CodeLens 的价值不在于替开发者作出决定，而在于帮助开发者更早看见风险、更完整理解变化，并在掌握充分信息后作出更好的决定。
