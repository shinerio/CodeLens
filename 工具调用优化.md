## 1. CodeLens 当前其实已经支持并发执行

  这次运行的平均值是：

  - 91 次工具调用
  - 71 次 LLM 响应
  - 平均每个响应只有 1.28 个工具调用
  - 51 个响应只有1个工具
  - 20个响应有2个工具
  - 从未一次调用3个以上

  但本地使用的 OpenAI Agents SDK 会把同一响应中的多个 Function Tool 创建为独立 asyncio.Task 并发执行；未设置上限时，默认并发槽位等于待执行工具数，参见本地 backend/.venv/lib/
  python3.12/site-packages/agents/run_internal/tool_execution.py:1402。

  问题在于：

  - backend/src/codelens/review/infrastructure/provider_adapters.py:41 没有显式设置 parallel_tool_calls=True。
  - Transcript 中实际参数是 parallel_tool_calls=null，完全依赖 DeepSeek默认行为。
  - Prompt 没有告诉模型哪些阶段必须批量调用。
  - 工具都是单路径形态，模型要主动生成多个 Function Call 才能批量。
  - 当前没有单轮工具总返回字节上限；如果突然并行8个、每个最多64 KiB，单轮可能塞回512 KiB。

  DeepSeek官方API本身允许 tool_choice=required 调用一个或多个工具，其Function Calling也支持并行调用。因此并不是供应商协议限制。DeepSeek Chat Completion文档
  (https://api-docs.deepseek.com/api/create-chat-completion)

  ## 2. Claude和Codex一类Agent怎么做

  ### Claude模式

  Claude API默认允许一个assistant响应包含多个 tool_use block。宿主应：

  1. 收集同一响应里的所有调用。
  2. 对独立只读调用使用 asyncio.gather/Promise.all。
  3. 把所有 tool_result 合并到下一条消息中。
  4. 保持调用ID和结果一一对应。

  Anthropic还明确建议：

  - 独立只读操作适合并发。
  - 有副作用、共享状态或顺序依赖的操作应串行。
  - 如果模型不主动并行，应增加明确Prompt。
  - 如果仍然不理想，可以提供一个封装多个调用的“批量工具”。Claude并行工具文档 (https://platform.claude.com/docs/en/agents-and-tools/tool-use/parallel-tool-use)

  Claude还提供Programmatic Tool
  Calling：模型生成受限代码，在代码执行容器里连续或并行调用多个工具、过滤结果，最后只把缩减后的结果送回模型。官方给出的搜索评测数据是平均性能提高11%、输入token减少24%。Claude
  Programmatic Tool Calling (https://platform.claude.com/docs/en/agents-and-tools/tool-use/programmatic-tool-calling)

  ### Codex/OpenAI模式

  OpenAI Responses API支持 parallel_tool_calls；更新的模型还支持Programmatic Tool
  Calling，即模型生成JavaScript，在一次模型回合内调用多个合格工具并聚合结果。官方建议把它用于过滤、连接、去重、排序和聚合等不需要每一步重新进行模型判断的流程。OpenAI模型与工具
  调用指南 (https://developers.openai.com/api/docs/guides/latest-model)

  Codex产品内部的完整调度实现没有公开文档，不能断言它的每个内部细节。但当前Codex环境本身就体现了类似设计：一个模型回合可以生成程序化编排代码，在不重新调用模型的情况下执行多个工
  具，再只把汇总结果交还模型。

  ## 3. 对CodeLens最合适的是三层混合方案

  ### 第一层：直接并行Function Call

  适合：

  - 同时读取4个互不依赖的测试文件。
  - 同时读取几个已知符号所在范围。
  - 同时查询多个独立grep。
  - 第二次 task_done 返回16个缺失文件后的补读。

  不适合：

  - grep → 根据结果决定read_file
  - get_diff → 根据cursor继续下一页
  - comment → 根据接受结果决定是否重试
  - comment、retract_comment、task_done 与其他工具混在同一批
  - 两个可能改变Candidate状态的控制工具并行

  建议配置：

  ModelSettings(
      ...,
      parallel_tool_calls=True,
  )

  同时必须在 RunConfig 中增加：

  ToolExecutionConfig(
      max_function_tool_concurrency=4,
  )

  先从4开始，不建议直接8或无限制。

  这条运行最后16个文件用了8轮。如果每轮4个读取，只需要4轮；每轮8个则只需2轮。仅这一段就有机会减少4–6次高上下文LLM调用，约少发送27万–40万“报告口径”的输入token，尽管其中大部分可
  能是缓存token。

  ### 第二层：提供宿主级批量工具

  单纯依赖模型生成多个调用仍不够稳定。建议新增类似：

  {
    "tool": "read_ranges",
    "requests": [
      {
        "path": "a.py",
        "version": "current",
        "line_range": {"start_line": 10, "end_line": 80}
      },
      {
        "path": "b.py",
        "version": "current",
        "line_range": {"start_line": 120, "end_line": 190}
      }
    ]
  }

  关键约束：

  - 每批最多4–8项。
  - 整批共用64–96 KiB返回上限，而不是每项64 KiB。
  - 每项独立返回 success/partial/rejected。
  - 结果稳定排序。
  - 每项分别更新变更范围覆盖。
  - 部分失败不丢弃其他成功项。
  - 批内只允许只读证据工具。
  - 仍经过同一个Run级Limiter。

  相比多个并行Function Call，批量工具还有额外收益：

  - 只传一次Tool Result顶层信封。
  - 减少重复的path/version/status/diagnostics字段。
  - 宿主可以统一分配返回字节预算。
  - 不依赖模型是否擅长生成多个工具调用。
  - 更容易保证覆盖和资源边界。

  ### 第三层：程序化或确定性编排

  不建议马上给Reviewer开放通用代码执行环境，这会扩大当前“无Shell、无网络、只读Snapshot”的安全边界。

  更适合CodeLens的是一个受限的宿主编排器：

  模型选择调查意图
    → 宿主并行读取多个冻结范围
    → 宿主做确定性的排序、分页、去重
    → 原始高信号证据交给模型判断

  程序化处理可以用于：

  - 合并多个grep结果。
  - 去重相同文件范围。
  - 根据 review_files 计算尚未覆盖的changed ranges。
  - 聚合测试与实现文件。
  - 按导入关系或目录把文件分组。
  - 过滤已经读取过且哈希相同的证据。

  不应该用于：

  - 让程序自动判断代码是否有缺陷。
  - 在原始证据到达模型前基于启发式丢弃可疑变更。
  - 自动摘要掉Finding所依赖的具体代码。

  ## 4. 现有 get_diff 已经是一个潜在批量工具，但Prompt没有利用好

  get_diff(path="", cursor=null) 理论上可以按稳定顺序返回根作用域下多个文件的完整hunk，而这次模型主要对单个文件调用：

  - get_diff 26次
  - read_file 53次
  - 其中8个核心文件既读过完整/局部正文，又读取过diff

  更合理的Reviewer流程应是：

  第一阶段：breadth pass
    get_diff(root, cursor=null)
    → 按cursor继续，直到完整覆盖所有可分页hunk

  第二阶段：deep dive
    对可疑hunk批量read_file上下文
    并行grep独立符号

  第三阶段：comments
    comment / retract_comment

  第四阶段：completion
    覆盖完整后task_done

  Prompt应明确规定：

  > 首先使用根或目录级 get_diff 完成广度覆盖。只有可疑hunk、超大hunk建议范围或需要调用方上下文时才使用 read_file。同一轮最多并行调用4个相互独立的只读证据工具。

  这可能比新增工具更早获得收益。不过必须先修复之前发现的超大hunk游标问题。

  ## 5. 并行化不能单独实施，因为会产生新的token峰值

  假如只加：

  parallel_tool_calls=True

  模型可能一次返回8个 read_file，每个最多64 KiB：

  8 × 64 KiB = 512 KiB

  这会导致下一次请求突然增加十几万token，并且当前compaction会保留最近6条证据，暂时压不掉它们。

  因此必须同时增加：

  - 单轮最大并行工具数：建议4。
  - 单轮工具结果总字节：建议64–96 KiB。
  - 单个结果继续保持现有上限。
  - 明确“只并行独立读取”。
  - 控制工具串行。
  - 批量结果一次性写入模型历史。

  ## 6. 比并行调用更进一步的其他优化

  ### A. 把 task_done 从模型可探测工具变成条件可用工具

  当前Prompt禁止提前调用，但模型仍调用两次。

  理想设计：

  - 覆盖不完整时不向模型暴露 task_done，或者调用必然不消耗一次完整模型恢复回合。
  - 每个证据结果返回有界进度：

  {
    "remaining_changed_range_count": 18,
    "remaining_file_count": 7
  }

  - 只有范围覆盖完成后，才启用 task_done。

  若结合 tool_choice=required，Reviewer调查轮可以只产生工具调用，减少普通文本输出。但要保证 task_done 不会被提前选择。

  ### B. 采用“范围级覆盖”，避免伪覆盖

  并行补读必须和覆盖修复一起做：

  - read_file只登记实际返回的完整行。
  - partial不应把整个文件标记完成。
  - 覆盖对象是changed ranges/hunks，而不是文件路径。
  - 批量工具逐项更新覆盖。

  否则批量化只会更快地产生“50/50完成但部分变更没读”的假象。

  ### C. 使用阶段Checkpoint代替49次小步上下文改写

  当前Evidence Compaction触发49次。根据缓存token变化，可以观察到6次明显的缓存前缀回退，累计下降105,472 cached token。虽然不能从Transcript严格证明每次下降都由压缩造成，但时序
  和“改写旧消息”的实现高度一致。

  更好的方式：

  1. 累积到阶段阈值。
  2. 保存结构化调查Checkpoint：
      - 已覆盖范围
      - 活跃Candidate
      - 已否定假设
      - 尚待调查符号
      - 必要证据引用

  3. 开始一个新的短上下文阶段。
  4. 在新阶段保持稳定前缀，不再每轮改写旧消息。

  这样缓存只在阶段切换时失效一次，而不是持续抖动。

  ### D. 引入冻结Snapshot上的符号/调用图工具

  这次模型多次在以下文件之间来回grep/read：

  - openai_runtime.py
  - tool_contract.py
  - evidence_replay.py
  - capability_tools.py
  - 对应测试

  一个只读的：

  get_symbol_context(symbol)
  → definition
  → inbound callers
  → outbound calls
  → changed hunks
  → related tests

  可以把多轮grep和文件读取合并成一次高信号结果。

  不必接入第三方live CodeGraph；可以在冻结Snapshot上建立受限内部索引，继续遵守哈希、路径和只读边界。

  ### E. 对Review范围做确定性分片

  50个文件放进一个持续增长的Agent对话，会产生近似“历史越来越长 × 调用越来越多”的放大效应。

  可把关联文件确定性分成5–10个shard：

  - 每个shard包含实现、直接测试和关联Prompt/契约。
  - 每个Reviewer只接收自己的文件范围。
  - Candidate由宿主聚合和去重。
  - 最终Verifier只检查Candidate和必要证据，不重新读取全部文件。

  这比简单增加多个Reviewer更省成本。普通多Agent只会复制Prompt；“范围不重叠的shard”才可能真正降低总token。

  ## 7. 推荐实施顺序

  1. 修复变更范围级覆盖和超大hunk分页。
  2. 显式设置 parallel_tool_calls=True。
  3. 设置SDK并发上限4和单轮总返回字节上限。
  4. 修改Prompt：breadth-first根级diff、独立读取最多并行4个、禁止调查旁白。
  5. 增加 read_ranges 批量工具。
  6. 条件化 task_done。
  7. 将连续小步compaction改为阶段Checkpoint。
  8. 最后评估Snapshot符号图和范围分片。

  ### 预期区间

  如果只做并行调用，91个工具调用按实际依赖关系估计可把71次LLM调用降低到约35–45次。

  如果再加：

  - 根级diff广度扫描
  - 批量范围读取
  - 条件化完成
  - 抑制调查旁白

  更合理的目标是15–30次LLM调用，输入token从337万降到约80万–160万。这个区间是基于本次Transcript结构的工程估计，必须用同一批Review样本对Finding召回率、误报率和范围覆盖做A/B评测，
  不能只用token下降判定成功。